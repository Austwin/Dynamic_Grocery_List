extends layout

block content
  h1 #{title}

  p Do you really want to clear this List?

  form(method='POST' action='')
      div.form-group
      input#listid.form-control(type='hidden',name='listid', required='true', value=list._id )

      button.btn.btn-primary(type='submit') Clear